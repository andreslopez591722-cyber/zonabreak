Este proyecto usa MongoDB como única base de datos persistente.

Configura la cadena de conexión en el archivo .env del directorio raíz:

  MONGODB_URI=mongodb://127.0.0.1:27017/zonabreak

Para datos iniciales de demostración: npm run seed (desde la raíz del proyecto).
